[
  {
    "key": "path",
    "type": "input",
    "defaultValue": "",
    "templateOptions": {
      "label": "Location containing the source data files",
      "placeholder": "Please enter the path to your source directory",
      "description": "Full directory path of the directory containing the source data files",
      "required": true
    }
  },
  {
    "key": "delimiter",
    "type": "input",
    "defaultValue": "",
    "templateOptions": {
      "label": "Field delimiter",
      "placeholder": "Please enter the delimiter as being used inside the files",
      "description": "Used delimiter within the source data files",
      "required": true
    }
  },
  {
    "key": "dateFormat",
    "type": "input",
    "defaultValue": "d-M-yyyy",
    "templateOptions": {
      "label": "Date format",
      "placeholder": "Please enter the date format as being used inside the date fields",
      "description": "Default input date format as being used inside the source files",
      "required": true
    }
  },
  {
    "key": "useConnectorPrimaryPersonCalculation",
    "type": "checkbox",
    "defaultValue": false,
    "templateOptions": {
      "label": "Connector calculates primary person",
      "required": false
    }
  }
]